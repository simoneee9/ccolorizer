name: cmake-build
run-name: building with cmake
on: [push]

jobs:
  build:
    
    runs-on: ubuntu-24.04

    steps:
      # download the repo
      - name: checkout
        uses: actions/checkout@v4

      - name: install sail
        run: apt-get update && apt-get -y install libsail-c++-dev
      - name: configure
        run: ./configure.sh
      - name: build
        run: ./build.sh
